{
  "name": "bundle-discount-function",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild dist/index.js --bundle --outfile=dist/function.js --platform=node --format=esm --target=esnext",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@shopify/shopify_function": "~2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "esbuild": "^0.24.0"
  }
}
