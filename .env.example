# Shopify App Credentials
# Get these from your Shopify Partners Dashboard: https://partners.shopify.com
SHOPIFY_API_KEY=your_api_key_here
SHOPIFY_API_SECRET=your_api_secret_here

# Shopify API Configuration
SHOPIFY_API_SCOPES=write_products,write_customers,write_discounts,read_orders
SHOPIFY_API_VERSION=2024-01

# App URL Configuration
# CRITICAL: This must match EXACTLY with the URL configured in Shopify Partners Dashboard
# Format: https://your-domain.com (no trailing slash)
# For local development with ngrok: https://your-subdomain.ngrok.io
# For production: https://your-app-domain.com
SHOPIFY_APP_URL=https://your-app-url.com

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/shopibundle

# Encryption
# Random string for encrypting sensitive data
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_STRING=your_random_encryption_string_here

# Environment
NODE_ENV=production

# IMPORTANT NOTES:
# 1. SHOPIFY_APP_URL must match the "App URL" in Partners Dashboard exactly
# 2. In Partners Dashboard, set:
#    - App URL: https://your-app-url.com
#    - Allowed redirection URL(s): https://your-app-url.com/api/auth/callback
# 3. For local development, use ngrok or cloudflare tunnel to get HTTPS URL
# 4. Never commit the actual .env file with real credentials
